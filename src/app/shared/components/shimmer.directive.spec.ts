import { ShimmerDirective } from './shimmer.directive';

describe('ShimmerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShimmerDirective();
    expect(directive).toBeTruthy();
  });
});
